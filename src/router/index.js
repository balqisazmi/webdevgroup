/*import { createRouter, createWebHistory } from 'vue-router';
import Home from '../views/Home.vue'; // Import the Home component
import RailwayFeedback from '../views/RailwayFeedback.vue'; // Import other components as needed

const routes = [
  {
    path: '/',
    name: 'Home',
    component: Home,
  },
  {
    path: '/railway-feedback',
    name: 'RailwayFeedback',
    component: RailwayFeedback,
  },
  // Add more routes as needed
];

const router = createRouter({
  history: createWebHistory(process.env.BASE_URL),
  routes,
});

export default router;
*/